<div class="container">

    <div class="row">
        <div class="col-md-12">
            <h1>Feed the plant!</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            {{!-- Plants needing fed --}}
            <h2>Plants needing to be fed!</h2>
            <ul>
                {{#each plants}}
                    {{#unless devoured}}
                        {{> plants/plant-block devour=true}}
                    {{/unless}}
                {{/each}}
            </ul>

        </div>
        <div class="col-md-4 pruning">
            {{!-- Plant input --}}
            <div class="boxInside">
            <h2>Add a plant!</h2>
            <form class="create-form">
                <div class="form-group">
                    <label for="plant">What will you name your plant?</label>
                    <input type="text" id="plantName" name="name">
                </div>
                <button type="submit">Add your plant's name</button>
            </form>
            </div>
        </div>
        <div class="col-md-4">
            {{!-- Fed plants --}}
            <h2>Plants already fed!</h2>
            <ul>
                {{#each plants}}
                    {{#if devoured}}
                        {{> plants/plant-block devour=false}}
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>

</div>

